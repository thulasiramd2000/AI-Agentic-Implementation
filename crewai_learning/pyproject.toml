[project]
name = "crewai_learning"
version = "0.1.0"
description = "crewAI_learning using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[tools]==1.14.4"
]

[project.scripts]
crewai_learning = "crewai_learning.main:run"
run_crew = "crewai_learning.main:run"
train = "crewai_learning.main:train"
replay = "crewai_learning.main:replay"
test = "crewai_learning.main:test"
run_with_trigger = "crewai_learning.main:run_with_trigger"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "crew"
